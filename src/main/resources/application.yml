spring:
  datasource:
    url: jdbc:mysql://localhost:3306/workout_db
    username: user
    password: passwd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true